applications:
- name: cf-spring-autoscaler-demo
  memory: 512M
  # Add your name/initials to make the hostname unique
  host: cf-spring-autoscaler-demo
  # Currently defaults to PWS
  #domain: cfapps.io
  path: target/cf-intro-autoscale-demo-0.0.1-SNAPSHOT.war
